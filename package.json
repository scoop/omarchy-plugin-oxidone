{
  "name": "omarchy-plugin-oxidone",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "bun test",
    "lint": "eslint .",
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^9",
    "prettier": "^3",
    "husky": "^9",
    "lint-staged": "^16"
  },
  "lint-staged": {
    "*.{js,json,md}": "prettier --write",
    "*.js": "eslint --fix"
  }
}
